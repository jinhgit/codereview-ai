# 서버에 보관하는 Groq API Key (권장)
GROQ_API_KEY=gsk_your_key_here

# 포트 (기본 8787)
PORT=8787

# 프론트 오리진 (CORS)
CORS_ORIGIN=http://localhost:5173

# 서버 키가 없을 때 클라이언트가 X-Groq-Key 헤더로 키를 넘기는 것을 허용 (로컬 개발용)
ALLOW_CLIENT_KEY=true

# 요청 본문 최대 코드 길이 (문자)
MAX_CODE_CHARS=80000
