{
  "name": "codereview-ai",
  "private": true,
  "version": "1.0.0",
  "description": "CodeReview AI monorepo (web + BFF)",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "dev:web": "npm run dev --prefix web",
    "dev:server": "npm run dev --prefix server",
    "dev:all": "npm run dev:server & npm run dev:web",
    "build": "npm run build --prefix server && npm run build --prefix web",
    "build:web": "npm run build --prefix web",
    "build:server": "npm run build --prefix server",
    "start:server": "npm run start --prefix server",
    "install:all": "npm install --prefix server && npm install --prefix web"
  }
}
